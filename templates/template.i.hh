//----------------------------------*-C++-*----------------------------------//
/*!
 * \file   <pkg>/<class>.i.hh
 * \author <user>
 * \date   <date>
 * \brief  Invariants of class <class>
 */
//---------------------------------------------------------------------------//
// $Id$
//---------------------------------------------------------------------------//

#ifndef rtt_<spkg>_<class>_i_hh
#define rtt_<spkg>_<class>_i_hh

namespace rtt_<spkg>
{

// Check(! (type==END      || type==EXIT || type==ERROR) || text=="");
// Check(type!=OTHER       || text.size()==1);
// Check(type!=KEYWORD     || Is_Keyword_Text(text.c_str()));
// Check(type!=REAL        || Is_Real_Text(text.c_str()));
// Check(type!=INTEGER     || Is_Integer_Text(text.c_str()));
// Check(type!=STRING      || Is_String_Text(text.c_str()));

} // end namespace rtt_<spkg>

#endif // rtt_<spkg>_<class>_i_hh

//---------------------------------------------------------------------------//
//              end of <pkg>/<class>.i.hh
//---------------------------------------------------------------------------//
