// Some comments, etc.

#include "foo.cc"

#ifdef SGI
#pragma do_not_instantiate foo<int>::barf()
#endif

instantiate class foo<int>

instantiate function char void blah( char *c )

instantiate func int ostream& operator<<( ostream& o, int i )
instantiate func DynArray<int> ostream& operator<<( ostream& o, DynArray<int>& di )

// More comments.
