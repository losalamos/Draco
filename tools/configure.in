AC_INIT(nmlgen)
AC_CONFIG_AUX_DIR(../config)
AC_DRACO_ARGS

AC_PROG_INSTALL

installdirs='${libexecdir} ${bindir}'
installfiles='${libexecdir}/nmlgen ${libexecdir}/launchtest ${bindir}/draco'

AC_SUBST(installdirs)
AC_SUBST(installfiles)

AC_OUTPUT(Makefile)
