\def\ps@myheadings{%
    \let\@oddfoot\@empty\let\@evenfoot\@empty
    \def\@evenhead{August 20, 1999 \, / \, Page {\thepage \ of \pageref{lastpage}}\hfil\slshape\leftmark}%
    \def\@oddhead{{\slshape\rightmark}\hfil{August 20, 1998 \, / \, Page \thepage \ of \pageref{lastpage}}}%
    \let\@mkboth\@gobbletwo
    \let\sectionmark\@gobble
    \let\subsectionmark\@gobble
    }
