//----------------------------------*-C++-*----------------------------------//
// pcg_DB.prs
// Dave Nystrom
// Fri May  9 09:45:08 1997
//---------------------------------------------------------------------------//
// @> pcg_DB template file for Perstalyzer.
//---------------------------------------------------------------------------//

#define pwrite(a) prst_write( ofs, #a, a )
#define pread(a)  prst_read(  ifs, #a, a )

// PRST: dump_command pwrite( %s );
// PRST: read_command pread(  %s );

//---------------------------------------------------------------------------//
// save()

// This method is responsible for dumping an object of class pcg_DB out to a
// file for permanent storage and subsequent retrieval, etc.
//---------------------------------------------------------------------------//

void pcg_DB::save( ofstream& ofs )
{
// PRST: dumpvars
}

//---------------------------------------------------------------------------//
// restore()

// This method is responsible for reading an object of class pcg_DB back from
// a file, in order to reinstantiate it just as it had been before.
//---------------------------------------------------------------------------//

void pcg_DB::restore( ifstream& ifs )
{
// PRST: readvars
}

//---------------------------------------------------------------------------//
//                              end of pcg_DB.prs
//---------------------------------------------------------------------------//
