puts "Running tests of linear algebra tBandMat component."

load_lib draco.exp

global ENV

puts "value of ENV is $ENV"

puts "Running tBandMat_$ENV"

set output [exec mpirun -np 4 tBandMat_$ENV]

# Check the output of tS1

if {[regexp "t1: passed" $output]} {
    pass t1
} else {
    fail t1
}

