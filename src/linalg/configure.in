dnl-------------------------------------------------------------------------dnl
dnl draco/src/linalg configure.in
dnl Randy M. Roberts
dnl 
dnl-------------------------------------------------------------------------dnl
dnl @> configure.in for linalg
dnl-------------------------------------------------------------------------dnl
dnl $Id$
dnl-------------------------------------------------------------------------dnl

AC_INIT(PCG_Ctrl.hh)
AC_CONFIG_AUX_DIR(../../config)
AC_CONFIG_HEADER(linalg/config.h:config.h.in)
package="linalg"

AC_PCG_SETUP(pkg) dnl through linalg
AC_MPI_SETUP(pkg)    dnl through c4
AC_SHMEM_SETUP(pkg)  dnl through c4
AC_LAPACK_SETUP(pkg) 

AC_INSTALL_HEADERS
AC_INSTALL_LIB
AC_NEEDS_LIBS(c4 ds++)

dnl AC_NEEDS_LIBS_TEST(nml)
AC_RUNTESTS(TstPCG, 1 2 5)

AC_DRACO_ENV

AC_OUTPUT(Makefile:../../config/Makefile.package.in \
test/Makefile:../../config/Makefile.test.in)

dnl-------------------------------------------------------------------------dnl
dnl			 end of draco/src/linalg configure.in
dnl-------------------------------------------------------------------------dnl
