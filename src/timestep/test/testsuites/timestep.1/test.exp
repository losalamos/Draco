puts "Running test of timestep component."

load_lib draco.exp

global ENV

puts "value of ENV is $ENV"

puts "Running main_$ENV"

set output [exec ./main_$ENV]

# Check the output of t1

if {[regexp "PASSED" $output]} {
    pass timestep
} else {
    fail timestep
}

# All done with tests of timestep package.
